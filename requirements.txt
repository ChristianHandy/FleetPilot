# Web Framework
flask>=3.0.0

# SSH Client
paramiko>=3.4.0

# Task Scheduler
apscheduler>=3.10.4

# System Monitor — CPU, RAM, disk, network and temperature metrics
psutil>=5.9.0

# For environment variable loading (optional but recommended)
python-dotenv>=1.0.0

# HTTP requests for version checking
requests>=2.31.0

# CSRF Protection (Security fix)
flask-wtf>=1.2.0

# Response compression — Gzip/Brotli (Performance)
flask-compress>=1.14

# Gunicorn WSGI server
gunicorn>=21.2.0

# 2FA / TOTP support
pyotp>=2.9.0

# YubiKey support (optional)
yubico-client>=1.11

# USB HID (for Arctic Fan Controller and other USB devices)
# Note: on Raspberry Pi install via: sudo apt install python3-hid
# hid>=1.0.4
